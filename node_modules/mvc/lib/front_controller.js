//import modules
var url = require('url'),
    stringUtils = require('./utils/string'),
    fs = require('fs'),
    path = require('path'),
    router = require('./router'),
    oo = require('./utils/oo'),
    Config = require('./config');


var FrontController = function(intent){
    this._initFrontController(intent);
};

FrontController.prototype = {
    _initFrontController: function(intent){
        this.intent = intent;
    },

    route: function(){
        var self = this;
	    router.dispatch(this.intent);
    }
}

module.exports = FrontController;
